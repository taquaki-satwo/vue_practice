import Vue from 'vue'

const app = new Vue({ // eslint-disable-line no-unuserd-vars
  el: '#app',
  data: {
    activeColor: 'blue',
    fontSize: 30,
    styleObject: {
      color: 'green',
      fontSize: '40px'
    }
  }
})
